# Документация API проекта Акселион

## Основные функции калькулятора

### Класс Calculator

#### __init__()
Инициализация калькулятора.
`python
def __init__(self):
    self.history = []
    self.current_result = 0
#### dd(a, b)
Сложение двух чисел.
**Параметры:**
-  (float): Первое число
-  (float): Второе число
**Возвращает:** float - результат сложения

#### subtract(a, b)
Вычитание двух чисел.
**Параметры:**
-  (float): Уменьшаемое
-  (float): Вычитаемое
**Возвращает:** float - результат вычитания

#### multiply(a, b)
Умножение двух чисел.
**Параметры:**
-  (float): Первый множитель
-  (float): Второй множитель
**Возвращает:** float - результат умножения

#### divide(a, b)
Деление двух чисел.
**Параметры:**
-  (float): Делимое
-  (float): Делитель
**Возвращает:** float - результат деления
**Исключения:** ValueError при делении на ноль

### Функции работы с историей

#### dd_to_history(operation, result)
Добавление операции в историю.
**Параметры:**
- operation (str): Описание операции
- esult (float): Результат операции

#### show_history()
Отображение истории вычислений.
**Возвращает:** str - форматированная история операций

#### clear_history()
Очистка истории вычислений.
